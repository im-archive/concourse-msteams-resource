---
resource_types:

- name: msteams-notifier
  type: registry-image
  source:
    repository: docker.pkg.github.com/wtw-im/concourse-msteams-resource/msteams-resource
    tag: latest
    username: ((github.token))
    password: x-oauth-basic

resources:

- name: notify-msteams
  type: msteams-notifier
  source:
    url: ((msteams.test_url))
    log_level: debug

jobs:

- name: test-msteams-resource
  plan:
  - put: notify-msteams
    params:
      title: This is a test
      text: This was generated from a concourse pipeline!
      summary: Test

